{
  "name": "ethereum-land-registration-gas-analysis",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo scaffold for performance and feasibility analysis of gas cost in Ethereum-based smart contracts: land registration case study.",
  "workspaces": [
    "apps/backend",
    "apps/frontend",
    "packages/contracts"
  ],
  "scripts": {
    "install:all": "npm install",
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "keywords": [
    "ethereum",
    "solidity",
    "hardhat",
    "ganache",
    "land-registration",
    "gas-analysis"
  ],
  "license": "MIT"
}
